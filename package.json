{
  "name": "freerouter-chatbot-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Sample chatbot powered by FreeRouter — swap between FreeRouter and OpenRouter by changing the base URL and key.",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "smoke": "node smoke.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.18.2"
  }
}
